@using Sandbox.UI;

@inherits Panel;

<root>
    @for (int i = 0; i < 9; i++)
    {
        <InventoryIcon Index="@i" Parent="@this" PlayerInventory="@(Player.FindLocalPlayer().GetComponent<PlayerInventory>())" />
    }
</root>

